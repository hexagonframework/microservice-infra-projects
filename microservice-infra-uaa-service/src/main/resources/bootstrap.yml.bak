info:
  component: UAA Service
spring:
  application:
    name: uaa-service
  cloud:
    config:
      uri: ${config.url}
      fail-fast: true
      username: ${config.username}
      password: ${config.password}
